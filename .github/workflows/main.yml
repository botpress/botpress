name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@master
    - uses: actions/setup-node@v1
    - run: | 
        cd docs/guide/website
        yarn
        yarn build
    - uses: ./.github/actions/check404
