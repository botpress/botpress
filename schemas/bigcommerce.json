{
  "actions": {
    "syncProducts": {
      "description": "Get all products from BigCommerce and sync them to a Botpress table",
      "input": { "schema": { "additionalProperties": false, "properties": {}, "type": "object" } },
      "output": {
        "schema": {
          "additionalProperties": false,
          "properties": {
            "message": { "description": "Status message", "type": "string" },
            "productsCount": { "description": "Number of products synced", "type": "number" },
            "success": { "description": "Whether the sync was successful", "type": "boolean" }
          },
          "required": ["success", "message", "productsCount"],
          "type": "object"
        }
      },
      "title": "Sync Products"
    }
  },
  "description": "Sync products from BigCommerce to Botpress",
  "interfaces": {},
  "name": "bigcommerce",
  "states": {},
  "title": "BigCommerce",
  "version": "3.0.1"
}
