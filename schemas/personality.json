{
  "configuration": {
    "schema": {
      "additionalProperties": false,
      "properties": {
        "model": { "description": "Model to use to handle bot personality", "type": "string" },
        "personality": {
          "description": "Describe what your chatbot is meant to do and how it should behave. You can include some personality traits here as well to influence how the chatbot will respond. Expressions are supported.",
          "maxLength": 1000,
          "type": "string"
        }
      },
      "required": ["model", "personality"],
      "type": "object"
    }
  },
  "conversation": { "tags": {} },
  "name": "personality",
  "user": { "tags": {} },
  "version": "0.0.1"
}
