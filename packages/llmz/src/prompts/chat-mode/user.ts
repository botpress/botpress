export default `■■■recap■■■

Output the next protocol blocks now. Start your output with ■.
For tool calls, start directly with ■run and stop to await its result. Add a progress message only if explicitly requested or required by an applicable example, and only if no explicit instruction forbids it. ALL messages, including progress updates, require a ■send=<component> block before ■run.
For the final answer, MATCH the casing and style of an applicable example (including ALL CAPS) unless explicit instructions override it. Use ■send=<component>, then ■next=<exit> to finish.
Output only the blocks themselves. Do not wrap or close the response with Markdown code fences.
`
