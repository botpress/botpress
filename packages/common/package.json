{
  "name": "@botpress/common",
  "version": "0.0.1",
  "main": "lib/index.js",
  "license": "AGPL-3.0",
  "repository": "https://github.com/botpress/botpress.git",
  "scripts": {
    "build": "yarn && tsc --build",
    "watch": "yarn && tsc --build --watch",
    "prepublishOnly": "yarn && tsc --build"
  },
  "publishConfig": {
    "@botpress:registry": "https://npm.pkg.github.com"
  },
  "types": "src/index.ts",
  "files": [
    "lib"
  ],
  "devDependencies": {
    "@types/node": "^10.10.3"
  },
  "dependencies": {}
}
