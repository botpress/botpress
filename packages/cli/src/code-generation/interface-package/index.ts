import * as client from '@botpress/client'
import * as types from '../typings'

export const generateInterfacePackage = async (_apiInterface: client.Interface): Promise<types.File[]> => {
  return []
}
