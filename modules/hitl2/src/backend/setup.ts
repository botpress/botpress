import * as sdk from 'botpress/sdk'

export const setup = async (bp: typeof sdk, botId: string, state): Promise<void> => {}

export const teardown = async (bp: typeof sdk, botId: string, state): Promise<void> => {}
