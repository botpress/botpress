{
  "type": "object",
  "properties": {
    "enabled": {
      "description": "Wheter the module is enabled or not.",
      "default": true,
      "type": "boolean"
    },
    "interval": {
      "description": "The interval between each analytics round.",
      "default": "30m",
      "type": "string"
    },
    "$schema": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "enabled",
    "interval"
  ],
  "$schema": "http://json-schema.org/draft-07/schema#"
}

