{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug Launcher-CE",
      "program": "${workspaceFolder}/src/bp/index.ts",
      "cwd": "${workspaceFolder}/src/bp",
      "protocol": "inspector",
      "env": {
        "JWT_SECRET": "very_secret",
        "NODE_PATH": "${workspaceFolder}/out"
      },
      "outFiles": ["${workspaceFolder}/out/**/*.js"]
    }
  ]
}
