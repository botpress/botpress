{
  "name": "@botpresshub/openai",
  "scripts": {
    "start": "pnpm bp serve",
    "build": "pnpm bp build",
    "deploy": "pnpm bp deploy",
    "check:type": "tsc --noEmit",
    "check:bplint": "pnpm exec bp lint",
    "add:interfaces": "bp add ../../interfaces/llm -y && bp add ../../interfaces/speech-to-text -y && bp add ../../interfaces/text-to-image -y"
  },
  "private": true,
  "dependencies": {
    "@botpress/client": "workspace:*",
    "@botpress/common": "workspace:*",
    "@botpress/sdk": "workspace:*",
    "openai": "^4.53.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.17",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
