import { getUserData } from './get-user-data'
import { customEvent, showWebchat, hideWebchat, toggleWebchat, configWebchat } from './webchat-events'

export default {
  getUserData,
  customEvent,
  showWebchat,
  hideWebchat,
  toggleWebchat,
  configWebchat,
}
