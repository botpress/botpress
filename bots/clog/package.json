{
  "name": "@bp-bots/clog",
  "scripts": {
    "postinstall": "genenv -o ./.genenv/index.ts -e SLACK_CLIENT_ID -e SLACK_CLIENT_SECRET -e SLACK_SIGNING_SECRET -e SLACK_REFRESH_TOKEN",
    "check:type": "tsc --noEmit",
    "check:bplint": "bp lint",
    "build": "bp add -y && bp build"
  },
  "private": true,
  "dependencies": {
    "@botpress/client": "workspace:*",
    "@botpress/sdk": "workspace:*",
    "@bpinternal/genenv": "0.0.1",
    "cheerio": "^1.1.2"
  },
  "bpDependencies": {
    "slack": "../../integrations/slack"
  }
}
