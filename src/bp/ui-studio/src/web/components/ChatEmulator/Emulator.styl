.msgInput {
  width: 100%;
  height: 50px;
  outline: none;
  border: none;
  resize: vertical;
  padding: 5px;
  border-top: 2px solid var(--c-neutral);

  &:focus {
    border-top: 2px solid var(--c-brand);
    background-color: var(--c-background-light-2);
  }

  &.disabled {
    opacity: 0.5;
  }
}

.container {
  display: flex;
  flex-direction: column;
  height: calc(100% - 24px);
}

.panes {
  position: relative;
  height: 100%;
}

.history {
  width: 100%;
  overflow-y: scroll;
  overflow-x: hidden;
}

.inspector {
  overflow-x: hidden;
  background-color: var(--c-background-dark-2);
  height: 100%;
  width: 100%;
  & > ul {
    margin: none;
    padding-left: 5px;
  }
}

.toolbar {
  background-color: var(--c-background--dark-1);
  border-top: 1px solid var(--c-background--dark-2);

  :global(.btn-default) {
    color: var(--c-neutral--light-1);
    background-color: var(--c-background--dark-1);
    border-color: var(--c-background--dark-2);
    padding: 3px 5px;
    border-radius: 0;
    border-top: none;
    border-bottom: none;

    &:hover {
      color: var(--c-text--light);
      background-color: var(--c-background--dark-2);
    }
  }

  :global(.glyphicon) {
    color: var(--c-text--light);
    font-size: 80%;
    margin-right: 3px;
  }
}

:global(.Resizer.horizontal) {
  background: black;
  height: 8px;
  cursor: row-resize;
  border-top: 3px solid rgba(255, 255, 255, 0) !important;
  border-bottom: 3px solid rgba(255, 255, 255, 0) !important;
  opacity: 0.1;
  margin-top: -7px;
  padding-top: 5px;

  &:hover {
    opacity: 0.2;
  }
}
