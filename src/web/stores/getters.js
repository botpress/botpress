const modules = ['modules']
const notifications = ['notifications']
const UI = ['UI']
const botInformation = ['botInformation']
const user = ['user']
const rules = ['rules']

export default { modules, notifications, UI, botInformation, user, rules }
