const modules = ['modules']
const notifications = ['notifications']
const UI = ['UI']
const botInformation = ['botInformation']
const license = ['license']
const user = ['user']
const rules = ['rules']

export default { modules, notifications, UI, botInformation, license, user, rules }
